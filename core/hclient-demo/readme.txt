运行说明：

1. 本示例依赖库为
	lib/org-json.jar
	lib/hurricane/hu-common-0.0.1-SNAPSHOT.jar
	lib/hurricane/hu-client-0.0.1-SNAPSHOT.jar

2. 运行本示例需要的配置文件为
	conf/config.client
并请将config.client中的IP地址修改为合适的服务器地址

3. 要求服务器端必须存在email对象，且该对象已正常生效。